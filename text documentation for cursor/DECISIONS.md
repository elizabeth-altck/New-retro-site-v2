# DECISIONS

Этот документ хранит ВСЕ важные решения проекта.

Он не является списком задач.

Он не является списком идей.

Он отвечает на вопрос:

«Почему проект выглядит именно так?»

Записывать сюда только те решения, которые влияют на дальнейшую разработку.

---

# Шаблон записи

## YYYY-MM-DD

### Решение

...

### Причина

...

### Последствия

...

---

# История решений

## 2026-06-27

### Решение

Главная страница является центральной комнатой всего мира GIF WORLD.

### Причина

Именно с неё пользователь начинает исследование сайта.

### Последствия

Все остальные страницы должны ощущаться как продолжение этого мира, а не отдельные сайты.

---

## 2026-06-27

### Решение

Навигация должна происходить не только через меню, но и через объекты страницы.

### Причина

Исследование мира интереснее обычной навигации.

### Последствия

Любая новая страница может использовать двери, окна, картинки, GIF, предметы или комнаты как способ перехода.

---

## 2026-06-27

### Решение

Разрешается использовать разные стили оформления на разных страницах.

### Причина

Каждая страница представляет собой отдельную часть цифрового мира.

### Последствия

Страница рабочего стола, гостиницы и музыкальной комнаты могут значительно отличаться визуально, сохраняя общую атмосферу проекта.

---

## 2026-06-24

### Решение

Ввести принцип **«миры внутри объектов»**: декоративные предметы на странице при взаимодействии открывают не попап, а маленький самостоятельный мир со своим оформлением и интерактивом.

### Причина

Это усиливает ощущение исследования, любопытства и «живого» цифрового места — в духе старого интернета и Neocities, где клик по объекту открывает неожиданное.

### Последствия

- При проектировании страниц предпочтительно использовать предметы-контейнеры вместо стандартных модальных окон без атмосферы.
- Каждый такий мир может иметь собственный визуальный стиль, но должен ощущаться частью GIF WORLD.
- Конкретные объекты по страницам — в `IDEAS.md` (раздел «Миры внутри объектов»).

---

## 2026-06-24

### Решение

Макеты Figma (1920px) использовать только как **референс пропорций**, не переносить размеры буквально в CSS.

### Причина

Реальный viewport и читаемость на вебе отличаются от статичного макета; адаптация сохраняет дух, но не ломает вёрстку.

### Последствия

При сверке с макетом подгонять относительные соотношения (зазоры, крупность акцентов), а не копировать px один в один.

---

## 2026-06-24

### Решение

Шапка главной: полупрозрачная тёмная подложка на всю ширину; заголовок **old net vibe** с жёсткой вишнёвой тенью и кремовым бликом; справа — **два слоя** одного PNG-сердца (тёмный offset + светлый).

### Причина

Соответствие retro-макету и ощущению «old net»; двойное сердце даёт объём без отдельного ассета.

### Последствия

- Текст тикера остаётся project-specific («old net core…»), не как в макете.
- Зазор шапка → контент через `--header-content-gap`, без уменьшения padding layout.
- Детали размеров и цветов — в `STYLE_GUIDE.md`.

---

## 2026-06-24

### Решение

Главная: трёхколоночная сетка **15 / 70 / 15**; боковые панели тёмные с зелёным inset-светом по внешнему краю.

### Причина

Макет «комнаты»: навигация и «система» по бокам, центр — личное ядро сайта.

### Последствия

Новые блоки на главной добавляются в `.core`; события системы — в правую колонку (`#eventsList`).

---

## 2026-06-24

### Решение

Первая реализация «мира внутри объекта» на главной — мини-игра **SIGNAL CATCH** в пунктирной зоне под заголовком **my site**; достижения пишутся в live events справа.

### Причина

Сразу проверить принцип интерактива + связь центра и правой колонки, не дожидаясь отдельных object-popups.

### Последствия

Следующие object-worlds (коробка, конверт, монитор) проектировать по тому же паттерну: свой UI внутри зоны + опциональная обратная связь в систему.

---

## 2026-06-28

### Решение

Коробка-коллаж на главной: **два PNG** (`closed` / `opened`) в `images/memory-box/`, открытие по **наведению** (на touch — тап). Не отдельный попап — смена состояния картинки в контейнере слева в `.core`.

### Причина

Визуал из Figma как коллаж; пользователь хочет оценять объект на сайте и легко заменять PNG при итерации дизайна.

### Последствия

- Справа от коробки — `#coreObjectsSlot` для других объектов.
- Зернистость и эффекты — через CSS на контейнер, без правки PNG.
- Внутренний «мир» коробки (gif, текст) — следующий этап, не блокирует смену внешних PNG.

---

## 2026-06-28

### Решение

В `#coreObjectsSlot` — **forecast.exe (Win98)**: окно «today's Forecast» + наложенное «НОВАЯ КОЛЛЕКЦИЯ» с меню File / Edit / Search / Close и кнопкой Look up; попапы через `#win98Popup`, события в `#eventsList`.

### Причина

Макет Figma и принцип object-world: не голый попап, а «живое» окно в стиле Win98 внутри центральной комнаты.

### Последствия

- Look up и меню пока дают атмосферные попапы, не реальную навигацию в коллекцию.
- Close скрывает окно коллекции визуально (`is-closed`), не удаляя из DOM.
- Дальнейший «мир коллекции» можно встроить в Look up или отдельную страницу.

---

## 2026-06-28

### Решение

**Улитка** — отдельный object в `.core` (`#snailDragWrap`), не внутри слота: **ползает сама** по оливковому блоку; пользователь может **перетащить** — после отпускания продолжает ползать **от новой позиции**, без возврата назад.

### Причина

Живой, смешной персонаж сайта; перетаскивание — вмешательство пользователя, не основной способ движения. Возврат на старую позицию после drag созвучен с «отменой» и ломает ощущение присутствия.

### Последствия

- Ассет: `images/snail.png`, размер ~**320px** (как в макете).
- Hover: мягкое свечение + блёстки на раковине; без резкого блика.
- Тап: shy → иногда `snail.exe` → каждый 5-й тап «полёт»; ползание **не** по тапу.
- События (pet, kick, flight) — в `#eventsList`.
- Ползание и drag — **по всему viewport** (`position: fixed`, без лимита `.core`); стартовая позиция — между site TODO scrollport и forecast.exe, не поверх блока заметок.

---

## 2026-06-28

### Решение

Целевая **V1 главной** — не «идеальный сайт», а **полный вертикальный scroll по макету**: все основные секции свёрстаны (note, intro, image of day, the snail), навигация ведёт хотя бы на заглушки страниц.

### Причина

Много концептов и желание работать постоянно; без «готовой когда-нибудь версии» проект остаётся вечным кусочным прототипом. Глубина (tamagotchi, комнаты персонажей, коллекция) — **после** цельного scroll.

### Последствия

- Приоритет: закрыть scroll главной по макету, затем gallery;
- виджет улитки остаётся на главной; переход в «комнату Жанны» — отдельный этап;
- tamagotchi / блок «the snail» — секция на главной + будущие страницы Жанна / Анна;
- пасхалки и полировка object-worlds — фоновый режим после V1 scroll.

---

## 2026-06-28

### Решение

**FIELD RADIO** в левой колонке; **site TODO scrollport**; **stickers.exe** (мини horizontal scrollport); **WRITE YOUR NOTE** (локально + опционально FormSubmit через `data-endpoint`); коробка уменьшена до ~360px.

### Причина

Наполнение главной живыми блоками без строгого 1:1 макета; место под контент; заметки и музыка — частые паттерны old web / neocities.

### Последствия

- Плеер: один legacy-трек; позже 3–5 треков с переключением (см. `IDEAS.md`).
- Заметки: `localStorage` до подключения почты.
- Улитка: `position: absolute` на body, scroll с страницей; дефолт не перекрывает note-блок.

---

## 2026-06-24

### Решение

Нижняя часть главной: **field diary** (горизонтальный scrollport, позже вертикальный), **spam.exe** (бесконечные mini-окна только внутри `#adSpamField`), **welcome popup** по центру при загрузке (не fullscreen). Контент внизу: image of the day, the snail teaser, hotline.txt, guest book preview, core-footer.

### Причина

Наполнить scroll главной; spam как локальный эксперимент внизу страницы; popups в духе 2000s без захвата всего экрана.

### Последствия

- Fullscreen spam убран; генерация окон только в spam-поле.
- Картинки дневника и image of the day — замена `src` в `main.html` + файлы в `images/`.
- **Заглушки `.media-placeholder`** для фото дня, дневника и character slots — пока нет финальных артов; Жанна в блоке — `BIG SMACHNY snail.png`.
- Layout: симметричные боковые колонки `--sidebar-width`, `field_stats.txt` справа для баланса с FIELD RADIO.

---

## 2026-06-24 — error-room страницы + the snail links

### Решение

Отдельные HTML-страницы ошибок/заглушек по макету Figma: **`404.html`** (cold) для несуществующих URL; **`202` warm** для nav и ссылок персонажа (`gallery`, `diary`, `secret-door`, `zhanna`, `anna`, `tamagotchi`, `widget`). Фон — `забор цветы и спокойствие.jpeg`. Блок **the snail** — полный текст макета; все ссылки ведут на заглушки, не на `#`.

### Причина

Nav и персонаж должны открывать «место в мире», даже если контента ещё нет; 404 и «не создана» — разный вайб (cold vs warm).

### Последствия

- Замена заглушки = переписать один html-файл или добавить новую страницу по шаблону `css/error-room.css`.
- `widget.html` — заглушка про виджет; живая улитка остаётся на `main.html`.
- Image of the day / diary — `.media-placeholder` до своих артов.
- **Пути error-room (2026-07):** относительные `css/`, `images/` + inline `<base href="/">` на https (не file://) — работает и локально, и на Vercel 404.

---

## 2026-06-24 — симметрия боковых колонок

### Решение

Сетка: `var(--sidebar-width) 1fr var(--sidebar-width)` вместо `15fr 70fr 15fr`. Убран асимметричный padding у `.center`. Заголовки боковых колонок — `.sidebar-title`. Справа добавлен `#fieldStats` в стиле FIELD RADIO.

### Причина

После роста центра боковые колонки визуально разъехались в ширине и «весе» контента.

### Последствия

Левая и правая колонки всегда одной ширины; нижние dashed-блоки (radio / stats) зеркалят атмосферу.

---

## 2026-06-24 — документация обновляется агентом по смыслу

### Решение

Cursor-агент **сам** дополняет документацию (`DECISIONS.md`, `IDEAS.md`, `TODO.md`, `SITE_STRUCTURE.md`, `STYLE_GUIDE.md`, `DEVELOPMENT_LOG.md`), когда в чате появляются новые решения, идеи или смена направления — без ручного указания «запиши в доки» каждый раз.

### Причина

Автор не хочет каждый раз напоминать про доки; контекст теряется между сессиями.

### Последствия

- Решения → `DECISIONS.md`; идеи «может когда-нибудь» → `IDEAS.md`; статус задач → `TODO.md`; структура/ID → `SITE_STRUCTURE.md`; вайб/типографика → `STYLE_GUIDE.md`; хронология → `DEVELOPMENT_LOG.md`.
- Если запись лишняя или неверная — автор говорит «откати»; агент убирает или правит.
- Коммит доков — только когда автор просит (как и код).
---

## 2026-07-14 — Фаза 1: starter-pack медиа на главной

### Решение

Убрать `.media-placeholder` на `main.html`: локальный starter-pack GIF из **GifCities** (diary, characters, blinkies) + **своё** фото для image of the day (кроп `забор цветы и спокойствие.jpeg` → `images/field/image-of-day.jpg`). Не hotlink.

### Причина

Roadmap Фаза 1: главная должна выглядеть живой до отдельных страниц; свои арты можно подменить позже, сохранив имена файлов.

### Последствия

- Папки: `images/diary/`, `images/characters/`, `images/blinkies/`, `images/field/` + `images/SOURCES.md`.
- stickers.exe = horizontal GIF-strip; footer = blinkies + last updated.
- CSS `.media-placeholder` оставлен на случай будущих слотов.
- Следующее по roadmap: **gallery.html v1**.

---

## 2026-07-14 — welcome один раз; rose-курсор parked

### Решение

1. Welcome popup на `main.html` — только при **первом** визите (`localStorage` ключ `gifworld-welcome-seen`). Возврат с gallery/diary и т.д. не показывает снова.
2. Кастомный курсор с розой **отключён** на всех страницах; файлы `js/field-cursor.js` + `css/field-cursor.css` оставлены (PARKED). Идея не убита — можно вернуть или вынести в ветку `experiment/rose-cursor` при коммите.

### Причина

Повторный popup при каждом заходе на главную раздражает. Курсор — классная идея, но пока мешает / не уверена; лучше убрать с main, не удаляя код.

### Последствия

- Сброс welcome: в DevTools → Application → Local Storage → удалить `gifworld-welcome-seen`.
- Вернуть курсор: подключить CSS+JS+`#fieldCursor` как раньше (или ветка).

---

## 2026-07-14 — gallery.html v1

### Решение

`gallery.html` — отдельная **комната** (`css/gallery.css`), не error-room 202. GIF-стена + marquee + scrollport. Фон через `--gallery-bg` (пока `поле.jpeg`), легко сменить без перестройки страницы.

### Причина

Фаза 2: первая настоящая страница после главной; фон автор ещё выбирает.

### Последствия

- Свой фон: файл → `images/gallery-bg.jpeg`, в CSS: `--gallery-bg: url("../images/gallery-bg.jpeg");`
- Дальше Фаза 2: `diary.html` v1.

---

## 2026-07-14 — diary.html v1

### Решение

`diary.html` — комната-тетрадь (`css/diary.css`): **field diary** (5 записей автора, вертикально) + **guest book** (имя необяз., сообщение, без регистрации). Одобренные записи в `data/guestbook.json`; форма — `data-endpoint` на `#guestbookForm` (FormSubmit / Web3Forms). До endpoint — «поле получило · после проверки».

### Причина

Фаза 2, пункт 2; guest book preview на main вёл в никуда; идея — впечатления о сайте без аккаунта.

### Последствия

- Новые одобренные записи: правка `data/guestbook.json` вручную (v1) или Supabase позже.
- Preview на main → `diary.html#guestbook`.
- WRITE YOUR NOTE на main — другая механика (личная записка), не смешивать.

---

## 2026-07-14 — korenara scrapbook: акценты, не замена вайба

### Решение

Референс [korenara Home](https://korenara.neocities.org/) (cream-коллаж, блокнот, ленты) — **вписывать в текущий вайб поля** (тёмные комнаты, golden `.core`, GIF WORLD), а не заменять gallery/diary целиком на «песочный scrapbook». **Desktop / explorer** — отдельная страница позже.

### Причина

Полная смена на cream scrapbook ломает уже сложившийся look main + комнат.

### Последствия

- Откат `scrapbook.css` как оболочки комнат.
- Брать от korenara: коллажные блоки, notebook-sidebar, webring, counter — **точечно**, в палитре страницы.
- Отдельно: страница «рабочий стол» с OS-рамкой.

**Roadmap вписывания (зафиксировано 2026-07-14):**

| # | Что | Где |
|---|-----|-----|
| 1 | «Лист бумаги» внутри блока (cream widget, grain, offset shadow) | diary pads, guest book, mini-world |
| 2 | Notebook-sidebar (spiral, rotated cards, report header) | diary справа |
| 3 | Scrollport в рамке (inset, custom thumb) | site TODO, gallery, guest feed |
| 4 | Counter odometer + webring label | main footer ← **тест v1** |
| 5 | Стикеры absolute | комнаты, не nav |
| 6 | Desktop / Ramen Explorer | **отдельная страница** |

Принцип: поле + golden `.core` снаружи; korenara — **внутри виджетов**, не body целиком.

---

## 2026-07-21 — field-glyphs убраны с main

### Решение

Разбросанные **field-glyphs** (мелкие GIF между колонками и в шапке) **удалены с `main.html`**. Ассеты `images/chaos/`, `diary/`, `characters/` остаются — используются в gallery, blinkies, character peeks, stickers.exe.

### Причина

В узкой сетке 15/70/15 гифки неизбежно перекрывали контент или выглядели сжато в 20px-щелях. Лучше чистая главная, чем «наклеенные» GIF, которые мешают читать.

### Последствия

- GIF-декор на main: только **внутри виджетов** (peeks, blinkies, stickers, ad-spam, polaroid).
- Вернуть «случайные» гифки позже — в **отдельных зонах** (gallery, комнаты, korenara item #5 «стикеры absolute»), не в nav и не между колонками.
- Scrollport: field-glyphs → `[ ]` pending.
